# 布置清单核验 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 分组完成后，学生在手机上查看分到的教室和布置清单，逐项打勾完成；管理员实时查看各教室进度。

**Architecture:** 新增 TaskProgress 模型记录每间教室的清单进度；学生端用学号+手机验证身份后展示任务；管理员端在招募列表查看进度总览。

**Tech Stack:** FastAPI + SQLAlchemy + SQLite + Bootstrap 5

---

## 自适应逻辑

此模块仅在「已配置考场 AND 已完成分组」的招募中可用。未分组的招募不显示任务入口。

---

## 文件结构

| 文件 | 变更 |
|------|------|
| `app/main.py` | 新增 TaskProgress 模型 + 任务初始化/查询/完成 API |
| `app/static/student.html` | 新增「我的任务」视图（查教室+清单打勾） |
| `app/static/admin.html` | 招募列表和分组面板中显示进度 |

---

### Task 1: TaskProgress 模型 + 后端 API

**Modify:** `app/main.py`

- [ ] **Step 1: 添加 TaskProgress 模型**

在 RecruitmentGroupClassroom 模型之后添加：

```python
class TaskProgress(Base):
    """布置/恢复任务进度记录"""
    __tablename__ = "task_progress"
    id = Column(Integer, primary_key=True)
    recruitment_classroom_id = Column(Integer, nullable=False)
    item_key = Column(String(50), nullable=False)     # 如 "door_post"
    item_name = Column(String(100), nullable=False)   # 如 "张贴门帖"
    is_completed = Column(Boolean, default=False)
    is_auto_skip = Column(Boolean, default=False)     # 固定桌椅自动跳过
    completed_by = Column(Integer, nullable=True)     # registration_id
    completed_at = Column(DateTime, nullable=True)
    task_type = Column(String(20), default="setup")   # "setup" 或 "recovery"
```

- [ ] **Step 2: 添加标准清单定义 + 初始化函数**

在工具函数区添加：

```python
# 标准布置清单（8项）
STANDARD_SETUP_ITEMS = [
    ("door_post", "张贴门帖（核对门牌号）", False),
    ("forbidden_items", "设置禁带物品放置处", False),
    ("count_desks", "清点桌椅数量/补齐缺额", True),   # 固定桌椅跳过
    ("clean_room", "教室环境清理（黑板/窗帘/课桌）", False),
    ("check_clock", "核对时钟", False),
    ("check_broadcast", "检查广播声音（听够3分钟）", False),
    ("seat_labels", "张贴座位号", False),
    ("self_check", "自查（对照标准逐项确认）", False),
]


def init_task_progress(recruit_id: int, db: Session):
    """为已分组的教室创建布置清单进度记录"""
    rcs = db.query(RecruitmentClassroom).filter(
        RecruitmentClassroom.recruitment_id == recruit_id
    ).all()
    if not rcs:
        return

    # 删除旧记录
    rc_ids = [rc.id for rc in rcs]
    db.query(TaskProgress).filter(
        TaskProgress.recruitment_classroom_id.in_(rc_ids),
        TaskProgress.task_type == "setup"
    ).delete()

    # 获取教室信息
    cr_map = {}
    classroom_ids = [rc.classroom_id for rc in rcs]
    for cr in db.query(Classroom).filter(Classroom.id.in_(classroom_ids)).all():
        cr_map[cr.id] = cr.is_fixed_seats

    # 创建进度记录
    for rc in rcs:
        is_fixed = cr_map.get(rc.classroom_id, False)
        for item_key, item_name, skip_for_fixed in STANDARD_SETUP_ITEMS:
            tp = TaskProgress(
                recruitment_classroom_id=rc.id,
                item_key=item_key,
                item_name=item_name,
                is_auto_skip=(skip_for_fixed and is_fixed),
                is_completed=(skip_for_fixed and is_fixed),  # 自动跳过 = 已完成
                task_type="setup",
            )
            db.add(tp)

    db.commit()
```

- [ ] **Step 3: 添加任务 API 端点**

在路由区域添加：

```python
@app.post("/api/recruit/{recruit_id}/init-tasks")
async def init_tasks(request: Request, recruit_id: int, db: Session = Depends(get_db)):
    """初始化布置清单任务（管理员手动触发）"""
    check_admin_login(request)
    check_csrf(request)
    init_task_progress(recruit_id, db)
    return {"code": 0, "msg": "任务初始化成功"}


@app.post("/api/my-tasks")
async def get_my_tasks(
    student_id: str = Form(...),
    phone: str = Form(...),
    db: Session = Depends(get_db)
):
    """学生查看自己的布置任务（需学号+手机验证）"""
    if not (student_id.isdigit() and len(student_id) == 8):
        raise HTTPException(400, "学号格式错误")
    if not (phone.isdigit() and len(phone) == 11):
        raise HTTPException(400, "手机号格式错误")

    # 查询报名记录
    reg = db.query(Registration).filter(
        Registration.student_id == student_id,
        Registration.phone == phone
    ).first()
    if not reg:
        raise HTTPException(404, "未找到报名记录")

    # 查询该学生所在的分组
    member = db.query(RecruitmentGroupMember).join(
        RecruitmentGroup,
        RecruitmentGroupMember.group_id == RecruitmentGroup.id
    ).filter(
        RecruitmentGroupMember.registration_id == reg.id,
        RecruitmentGroup.recruitment_id == reg.recruitment_id
    ).first()

    if not member:
        return {"code": 0, "data": [], "msg": "暂无分组任务"}

    # 查询该组分配的教室
    assignments = db.query(RecruitmentGroupClassroom).filter(
        RecruitmentGroupClassroom.group_id == member.group_id
    ).all()

    # 获取教室信息和任务进度
    group = db.query(RecruitmentGroup).filter(RecruitmentGroup.id == member.group_id).first()

    result = []
    for assign in assignments:
        rc = db.query(RecruitmentClassroom).filter(RecruitmentClassroom.id == assign.recruitment_classroom_id).first()
        if not rc:
            continue
        cr = db.query(Classroom).filter(Classroom.id == rc.classroom_id).first()
        if not cr:
            continue

        # 查询该教室的布置任务进度
        tasks = db.query(TaskProgress).filter(
            TaskProgress.recruitment_classroom_id == rc.id,
            TaskProgress.task_type == "setup"
        ).order_by(TaskProgress.id).all()

        task_list = [{
            "id": t.id,
            "item_key": t.item_key,
            "item_name": t.item_name,
            "is_completed": t.is_completed,
            "is_auto_skip": t.is_auto_skip,
        } for t in tasks]

        completed = sum(1 for t in tasks if t.is_completed)
        total = sum(1 for t in tasks if not t.is_auto_skip)

        result.append({
            "rc_id": rc.id,
            "classroom_name": cr.name,
            "building_name": "",  # TODO: add building name
            "exam_numbers": [rc.exam_number_start, rc.exam_number_start + 1] if rc.exam_mode == "double" else [rc.exam_number_start],
            "tasks": task_list,
            "progress": f"{completed}/{total}",
            "all_done": completed >= total,
        })

    return {"code": 0, "data": result, "zone_name": group.zone_name if group else ""}


@app.post("/api/tasks/{task_id}/toggle")
async def toggle_task(
    request: Request,
    task_id: int,
    db: Session = Depends(get_db)
):
    """切换任务项的完成状态"""
    task = db.query(TaskProgress).filter(TaskProgress.id == task_id).first()
    if not task:
        raise HTTPException(404, "任务不存在")
    if task.is_auto_skip:
        raise HTTPException(400, "自动跳过的任务不可操作")

    task.is_completed = not task.is_completed
    task.completed_at = now_beijing() if task.is_completed else None
    db.commit()

    return {"code": 0, "msg": "更新成功", "is_completed": task.is_completed}


@app.get("/api/recruit/{recruit_id}/task-progress")
async def get_task_progress(request: Request, recruit_id: int, db: Session = Depends(get_db)):
    """管理员查看所有教室的进度"""
    check_admin_login(request)

    groups = db.query(RecruitmentGroup).filter(
        RecruitmentGroup.recruitment_id == recruit_id
    ).all()

    result = []
    for g in groups:
        assigns = db.query(RecruitmentGroupClassroom).filter(
            RecruitmentGroupClassroom.group_id == g.id
        ).all()

        for assign in assigns:
            rc = db.query(RecruitmentClassroom).filter(RecruitmentClassroom.id == assign.recruitment_classroom_id).first()
            if not rc:
                continue
            cr = db.query(Classroom).filter(Classroom.id == rc.classroom_id).first()
            if not cr:
                continue

            tasks = db.query(TaskProgress).filter(
                TaskProgress.recruitment_classroom_id == rc.id,
                TaskProgress.task_type == "setup"
            ).all()

            completed = sum(1 for t in tasks if t.is_completed)
            total = sum(1 for t in tasks if not t.is_auto_skip)

            result.append({
                "classroom_name": cr.name,
                "zone_name": g.zone_name or "无分区",
                "progress": f"{completed}/{total}",
                "percent": int(completed / total * 100) if total > 0 else 0,
                "all_done": completed >= total,
            })

    return {"code": 0, "data": result}
```

- [ ] **Step 4: 修改 auto-group 完成后自动初始化任务**

在 `save_groups_to_db` 函数末尾（`db.commit()` 和 `return` 之间），添加：
```python
    init_task_progress(recruit_id, db)
```

- [ ] **Step 5: 验证**

```bash
cd /Users/wff/KAIfa/kaowu-system/app && python3 -c "from main import app; print('OK')"
```

- [ ] **Step 6: 提交**

```bash
cd /Users/wff/KAIfa/kaowu-system && git add app/main.py && git commit -m "$(cat <<'EOF'
feat: add task checklist model and APIs

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: 学生端任务视图

**Modify:** `app/static/student.html`

- [ ] **Step 1: 在查询报名记录下方添加「我的任务」卡片**

在 `myList` 容器之后（`id="myList"` 的 `</div>` 后），添加：

```html
    <!-- 布置任务 -->
    <div class="card p-4 mt-4" id="taskCard" style="display:none;">
        <h3 class="mb-3">📋 我的布置任务</h3>
        <div id="taskList"></div>
    </div>
```

- [ ] **Step 2: 在查询成功后加载任务数据**

在 `myRegForm` 的 submit 处理函数中，在数据成功加载后（`data.code === 0` 分支末尾，`}` 之前），添加：

```javascript
                // 加载布置任务
                loadMyTasks(document.getElementById("myStudentId").value, document.getElementById("myPhone").value);
```

- [ ] **Step 3: 添加任务加载和操作函数**

在 script 块末尾添加：

```javascript
// ========== 布置任务 ==========

async function loadMyTasks(studentId, phone) {
    const card = document.getElementById('taskCard');
    const container = document.getElementById('taskList');

    const formData = new FormData();
    formData.append('student_id', studentId);
    formData.append('phone', phone);

    try {
        const res = await fetch('/api/my-tasks', { method: 'POST', body: formData });
        const data = await res.json();

        if (data.code !== 0 || !data.data || data.data.length === 0) {
            card.style.display = 'none';
            return;
        }

        card.style.display = 'block';
        let html = data.zone_name ? `<div class="mb-2 text-muted small">🏢 ${data.zone_name}</div>` : '';

        data.data.forEach(item => {
            const examNos = item.exam_numbers.join('、');
            html += `<div class="card border mb-3">
                <div class="card-header d-flex justify-content-between align-items-center py-2">
                    <span class="fw-bold">🏫 ${item.classroom_name}（${examNos}考场）</span>
                    <span class="badge ${item.all_done ? 'bg-success' : 'bg-warning text-dark'}">${item.progress}</span>
                </div>
                <div class="card-body py-2">`;

            item.tasks.forEach(t => {
                if (t.is_auto_skip) {
                    html += `<div class="d-flex align-items-center mb-1 text-muted">
                        <span style="width:24px;">⏭️</span>
                        <span class="small">${t.item_name}（已跳过）</span>
                    </div>`;
                } else {
                    const checked = t.is_completed ? 'checked' : '';
                    html += `<div class="form-check mb-1">
                        <input class="form-check-input task-checkbox" type="checkbox" data-task-id="${t.id}" ${checked}>
                        <label class="form-check-label small">${t.item_name}</label>
                    </div>`;
                }
            });

            html += `</div></div>`;
        });

        container.innerHTML = html;

        // 绑定复选框事件
        container.querySelectorAll('.task-checkbox').forEach(cb => {
            cb.addEventListener('change', async function() {
                const taskId = this.dataset.taskId;
                const label = this.nextElementSibling;
                try {
                    const res = await fetch(`/api/tasks/${taskId}/toggle`, { method: 'POST' });
                    const data = await res.json();
                    if (data.code === 0) {
                        label.style.textDecoration = data.is_completed ? 'line-through' : 'none';
                        // 刷新进度
                        setTimeout(() => loadMyTasks(studentId, phone), 500);
                    }
                } catch (err) {
                    this.checked = !this.checked;
                    showToast('操作失败', 'danger');
                }
            });
        });
    } catch (err) {
        card.style.display = 'none';
    }
}
```

- [ ] **Step 4: 验证**

```bash
cd /Users/wff/KAIfa/kaowu-system && python3 -c "open('app/static/student.html').read(); print('OK')"
```

- [ ] **Step 5: 提交**

```bash
cd /Users/wff/KAIfa/kaowu-system && git add app/static/student.html && git commit -m "$(cat <<'EOF'
feat: add task checklist view for students

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: 管理员端进度总览

**Modify:** `app/static/admin.html`

在招募列表操作列中添加「进度」按钮，或在分组面板中显示进度。

- [ ] **Step 1: 在分组面板中显示进度**

在 `renderGrouping` 函数中，在每个组卡片内添加进度条。找到教室名称显示的地方，在其后添加：

在 `card-body` 内的 `🏫 ${classroomNames || '未分配教室'}` 行之后，添加进度条 HTML。

由于分组 API 的返回数据目前不包含进度，先添加一个简单的标注——分组完成后提示管理员点击「初始化任务」。

- [ ] **Step 2: 在 openGroupingPanel 中添加初始化任务按钮**

在分组面板的 modal-body 顶部或 auto-group 按钮附近，添加条件提示——如果未初始化任务，显示一个按钮。

先不急着做复杂的进度面板，简单起见，在分组面板底部或保存后自动调用`/api/recruit/{id}/init-tasks`。

- [ ] **Step 3: 提交**

```bash
cd /Users/wff/KAIfa/kaowu-system && git add app/static/admin.html && git commit -m "$(cat <<'EOF'
chore: placeholder for admin task progress view

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```
